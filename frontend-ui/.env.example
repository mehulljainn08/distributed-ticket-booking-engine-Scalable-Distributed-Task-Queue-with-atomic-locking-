# API Gateway URL (Anand's service)
VITE_API_URL=http://localhost:5000

# Socket.IO Webhook Service URL (Dhruv's service)
VITE_SOCKET_URL=http://localhost:7000

# Enable mock mode when backend is not running (true/false)
VITE_MOCK_MODE=false
